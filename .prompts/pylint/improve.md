Please run...

```bash
source ./.venv/bin/activate
pylint ormantism
```

...and apply recommended fixes that do not require code suppression.

Also, ensure tests have not been broken:

```bash
source ./.venv/bin/activate
pytest tests
```
