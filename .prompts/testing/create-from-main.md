Please find occurrences of `if __name__ == "__main__":` in Python scripts present under the `ormantism` directory of the project.

Use these pieces of code as inspiration for writing/updating tests (if not already done).

You may run tests with:

```bash
source .venv/bin/activate
pytest tests/path/to/test.py
```
